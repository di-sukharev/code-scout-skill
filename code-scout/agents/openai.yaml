interface:
  display_name: "Code Scout"
  short_description: "A subagent researches code and reports findings"
  default_prompt: "Use $code-scout to research the current implementation, relevant dependencies, and tests, then continue the task from its concise report."
